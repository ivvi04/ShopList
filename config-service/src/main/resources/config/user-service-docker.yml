server:
  port: 0

#url, куда будет происходить регистрация микросервисов и другие важные действия
eureka:
  client:
    serviceUrl:
      defaultZone: http://server-service:8761/eureka/
  instance:
    hostname: user-service
    InstanceId: ${spring.application.name}:${random.uuid}
    preferIpAddress: true
#    leaseRenewalIntervalInSeconds: 5
#    leaseExpirationDurationInSeconds: 2

##формат вывода логов
#logging:
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] ${LOG_LEVEL_PATTERN:-%5p} %m%n"

springdoc:
  packagesToScan: ru.lakeevda.userservice

spring:
  datasource:
    url: "jdbc:postgresql://shop-list-db:5432/shoplist"
    driverClassName: "org.postgresql.Driver"
    username: postgres
    password: postgres